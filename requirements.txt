torch>=2.0.0
torch-geometric>=2.3.0
torchvision>=0.15.0          # transitive dep of torch-geo; keeps versions consistent

# Bioinformatics
scanpy>=1.9.6
squidpy>=1.2.3
anndata>=0.9.2

# Scientific Python
numpy>=1.24.0
pandas>=1.5.3
scipy>=1.10.0
scikit-learn>=1.2.2
statsmodels>=0.14.0          # used by cross-donor correlation in invariance.py

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.2

# Utilities
tqdm>=4.65.0
pyyaml>=6.0                  # for reading experiment configs
h5py>=3.8.0                  # .h5 and .h5ad I/O

# Optional — install separately if needed for downstream comparison:
# stagate_pyg >= 1.0.0       # pip install stagate_pyg
# GraphST                    # see https://github.com/JinmiaoChenLab/GraphST

# Testing
pytest>=7.3.0
